package com.swaglabs.drivers;

import org.openqa.selenium.WebDriver;

public abstract class AbstractDriver {
    // defines a method to start a WebDriver instance
    public abstract WebDriver startDriver();
}
